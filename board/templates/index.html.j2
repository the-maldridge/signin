{% extends "base.html.j2" %}

{% block title %}Sign in Board{% endblock %}

{% block body %}
    <div id="flex-container">
        <div id="absent">
            <div class="list-header transparent-box">
                Out
            </div>
            <div class="people-list">
            {% for person in people %}
                <a href="tap-in/{{ person.id }}" class="person transparent-box">
                    {{ person.name }}<br />
                    ({{ person.alias }})
                </a>
            {% endfor %}
            </div>
        </div>
        <div id="present">
            <a href="tap-all-out">
                <div class="list-header transparent-box">
                    In
                </div>
            </a>
            <div class="people-list">
            {% for person in present %}
                <a href="tap-out/{{ person.id }}" class="person transparent-box">
                    {{ person.name }}<br />
                    ({{ person.alias }})
                </a>
            {% endfor %}
            </div>
        </div>
    </div>
{% endblock %}

{% block baseboard %}
    <a href="log">
        <div class="transparent-box baseboard-button">
            Log
        </div>
    </a>
    <a href="new">
        <div class="transparent-box baseboard-button">
            New
        </div>
    </a>
{% endblock %}
